name: Push Success Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Success message
        run: echo "✅ Push successful! Portfolio updated."
